function onPortal(role){
// Replace alerts with actual navigation when wiring up routes
alert(role.charAt(0).toUpperCase() + role.slice(1) + ' login clicked');
}
function onRegister(){
//alert('Register clicked');
}